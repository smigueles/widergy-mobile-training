import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  input: {
    borderWidth: 1,
    borderColor: 'black',
    alignSelf: 'center',
    width: 200,
  },
  welcomeText: {
    alignSelf: 'center',
  },
});
